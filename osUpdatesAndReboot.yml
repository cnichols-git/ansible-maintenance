---
- hosts: all
  become: true
  tasks:

  - name: Update OS and NO RESTART
    apt: 
      update_cache: yes
    when: ansible_distribution == "Ubuntu"

  - name: Update OS and NO RESTART
    dnf: 
      update_cache:  yes
    when: ansible_distribution == "Rocky"

# Detect OS 
# update excluding kernel versions
# reboot without remorse


